= simple_form_for(@product) do |f|
  = f.error_notification

  .form-inputs
    = f.input :code
    = f.input :name
    = f.input :description
    = f.association :category

  .form-actions
    = f.button :submit
