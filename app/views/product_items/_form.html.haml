= simple_form_for(@product_item) do |f|
  = f.error_notification

  .form-inputs
    = f.input :code
    = f.input :quantity
    = f.input :last_check_in
    = f.input :last_checkout

  .form-actions
    = f.button :submit
