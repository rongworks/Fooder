%h1 Listing product_items

%table
  %tr
    %th Code
    %th Quantity
    %th Last check in
    %th Last checkout
    %th
    %th
    %th

  - @product_items.each do |product_item|
    %tr
      %td= product_item.code
      %td= product_item.quantity
      %td= product_item.last_check_in
      %td= product_item.last_checkout
      %td= link_to 'Show', product_item
      %td= link_to 'Edit', edit_product_item_path(product_item)
      %td= link_to 'Destroy', product_item, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Product item', new_product_item_path
